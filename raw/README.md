# raw/ — append-only evidence

This folder holds original, unprocessed evidence: exports Chris provides (e.g. the credit-memos CSV, bank-ledger CSVs, P&L PDFs), API dumps used to settle a question, screenshots of bugs.

**Rules:**
- Append only. Never edit, overwrite, or delete anything here — including by "cleanup".
- Every file gets a date prefix: `YYYY-MM-DD-<what-it-is>.<ext>`.
- When a raw file settles a question, the CONCLUSION goes in a `concepts/` or `entities/` page that cites the raw file; the raw file itself stays untouched.

Nothing is stored here yet. Files Chris shared during the build session (credit_memos_ytd.csv, the reference P&L PDF) lived only in that session's uploads — ask him to re-share if needed; their conclusions are already captured in `concepts/intercompany-boise-credit-memos.md` and `entities/decisions.md` (D10–D12).
